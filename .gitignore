# Hugo
/public/
/resources/_gen/
.hugo_build.lock

# Secrets and credentials (never commit)
.env
.env.*
!.env.example
secrets/
credentials.json
secrets.json
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.pub

# Cloudflare Worker (local only — deploy via Cloudflare dashboard)
/workers/

# Decap admin runtime config (generated in CI from secrets)
/static/admin/config.yml

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*~
